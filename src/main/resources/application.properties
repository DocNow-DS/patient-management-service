server.port=8081
spring.application.name=patient-management-service

# MongoDB
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:${MONGODB_URI:mongodb+srv://dbuser:dbuser2026@test.ubtlnvl.mongodb.net/client?appName=TEST}}
#spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:${MONGODB_URI:mongodb://localhost:27017/HealthCare}}
spring.data.mongodb.auto-index-creation=true

file.upload-dir=./uploads
report.max-file-size-mb=${REPORT_MAX_FILE_SIZE_MB:10}
report.allowed-extensions=${REPORT_ALLOWED_EXTENSIONS:pdf,jpg,jpeg,png,doc,docx}
report.fail-on-remote-upload-error=${REPORT_FAIL_ON_REMOTE_UPLOAD_ERROR:true}
cloudinary.enabled=${CLOUDINARY_ENABLED:true}
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.upload-preset=${CLOUDINARY_UPLOAD_PRESET:}
spring.servlet.multipart.max-file-size=${SPRING_MULTIPART_MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${SPRING_MULTIPART_MAX_REQUEST_SIZE:10MB}
logging.level.org.mongodb.driver.cluster=WARN

# JWT
# Recommended: set `JWT_SECRET` env var in production.
# Secret used to sign JWTs. For production, set the `JWT_SECRET` environment variable.
jwt.secret=${JWT_SECRET:dev-secret-change-me}
# 1 day in milliseconds
jwt.expiration-ms=${JWT_EXPIRATION_MS:2592000000}

# RabbitMQ (event-driven integration)
spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}

healthcare.user-events.exchange=${USER_EVENTS_EXCHANGE:healthcare.user.events}
logging.level.com.healthcare.patient.controller.AuthController=DEBUG


# Supabase configuration (replace with your project values)
supabase.url=${SUPABASE_URL:https://rmyjgsrbudpkqpcegruz.supabase.co}
supabase.service-key=${SUPABASE_SERVICE_KEY:sb_secret_73AqEbDz8CjcUP04WcHurg_T9vZayj4}
supabase.bucket=${SUPABASE_BUCKET:medicine-reports}